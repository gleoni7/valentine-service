# Valentine Service

A lightweight backend service for evaluating compatibility
and determining Valentine’s Day availability.

## Setup

```bash
git clone <repo-url>
cd valentine-service
pip install -r requirements.txt
python app.py
